apiVersion: user.openshift.io/v1
kind: Group
metadata:
  name: platform-admins
users:
  # Membership uses the OpenShift username, which equals the Google email address.
  # Provider uses mappingMethod: lookup, so each member needs a pre-provisioned
  # User + Identity (see oauth.yaml and the per-user manifests) before login works.
  - sean@vaughan.cc
  - svaughan@redhat.com
