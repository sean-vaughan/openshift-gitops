apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../../sources/openshift-authentication
  # Google OAuth client secret, sealed against this cluster's sealed-secrets key.
  - sealed-google-client-secret.yaml
